# نظام إدارة وجرد الأصول الحكومية
## Government Asset Inventory Management System

### الإصدار 6.5 - Version 6.5

---

## 📋 نظرة عامة | Overview

نظام متكامل لإدارة وجرد وحصر الأصول في الجهات الحكومية، يعمل على الويب والهاتف مع وضع offline ومزامنة تلقائية عند الاتصال. يدعم Firebase (Firestore وStorage) كمنصة سحابية مركزية.

---

## ✅ الميزات المكتملة | Completed Features

### 🔐 نظام المصادقة والصلاحيات
- **تسجيل الدخول**: صفحة تسجيل دخول آمنة (`login.html`)
- **Firebase Authentication**: دعم كامل لـ Firebase Auth
- **أربع مستويات صلاحيات**:
  - `admin` - المشرف العام: وصول كامل لجميع الفروع والبيانات والإعدادات
  - `manager` - مدير فرع: إدارة الأصول والعمال في فرعه فقط
  - `field_user` - عامل ميداني: إضافة الأصول أثناء الجرد ورؤية أصوله فقط
  - `client` - عميل: رؤية وتعديل بياناته الخاصة فقط
- **صلاحيات مخصصة**: إمكانية تخصيص صلاحيات دقيقة لكل مستخدم
- **إنشاء حساب للعميل**: يمكن للعملاء إنشاء حسابات جديدة بأنفسهم

### 📦 إدارة الأصول
- إضافة وتعديل وحذف الأصول
- تصنيف ثلاثي المستويات (رئيسي / فرعي / تفصيلي)
- معلومات تفصيلية (المورد، تاريخ الشراء، السعر، الحالة)
- ربط الأصول بالإدارات والمواقع
- مسح الباركود للأصول

### 📸 نظام ألبوم الصور
- تحميل حتى 10 صور لكل أصل
- ضغط تلقائي للصور
- عرض الصور في معرض تفاعلي
- **رابط واحد لعرض جميع صور كل أصل**: يُضمّن في ملف Excel
- فتح ألبوم الصور عبر رابط مباشر: `index.html?view=album&asset={assetId}`

### 📍 التسجيل الجغرافي (GPS)
- تحديد موقع الأصل أثناء الجرد
- حفظ الإحداثيات (Latitude/Longitude) ودقة GPS
- روابط Google Maps ديناميكية لكل أصل
- زر "تحديد الموقع" في نموذج إضافة الأصل

### 📊 جلسات الجرد
- إنشاء جلسات جرد جديدة لكل فرع
- تعيين العمال المشاركين
- تتبع تقدم الجلسة ونسبة الإنجاز
- ربط الأصول تلقائياً بالجلسة النشطة

### 👥 إدارة المستخدمين (للمشرف)
- إضافة وتعديل وتعطيل المستخدمين
- تعيين الأدوار والصلاحيات
- ربط المستخدمين بالفروع
- **صلاحيات مخصصة**:
  - رؤية كل الفروع
  - إدارة المستخدمين
  - حذف الأصول
  - تصدير البيانات
  - عرض التقارير
  - إدارة جلسات الجرد

### 🏢 إدارة الفروع
- إضافة فروع جديدة
- تعيين رمز مختصر لكل فرع
- إحصاءات أداء الفروع

### 📤 التصدير المحسن (Excel)
- تصدير الأصول إلى Excel مع جميع البيانات
- **روابط Hyperlinks في Excel**:
  - رابط Google Maps للموقع الجغرافي
  - رابط واحد لألبوم الصور لكل أصل
- ورقة ملخص مع معلومات الجلسة
- ورقة إحصائيات بتوزيع الأصول

### 🔔 نظام الإشعارات
- إشعارات عند إضافة أصول جديدة
- تنبيهات المزامنة
- إشعارات النظام والصيانة
- دعم إشعارات سطح المكتب

### 📶 العمل بدون إنترنت (PWA)
- تخزين محلي في IndexedDB
- مزامنة تلقائية عند الاتصال
- Service Worker للتشغيل offline
- قابل للتثبيت كتطبيق

---

## 🔗 الروابط والمسارات | URLs & Routes

### الصفحات الرئيسية
| المسار | الوصف |
|--------|--------|
| `login.html` | صفحة تسجيل الدخول |
| `index.html` | الصفحة الرئيسية / لوحة التحكم |
| `offline.html` | صفحة عدم الاتصال |

### معاملات URL
| المسار | الوصف |
|--------|--------|
| `index.html?view=album&asset={assetId}` | عرض ألبوم صور أصل محدد |
| `index.html?view=album&asset={assetId}&name={assetName}` | عرض ألبوم مع اسم الأصل |

### API Endpoints (RESTful Table API)
```
GET    tables/{table}              - قائمة السجلات
GET    tables/{table}/{id}         - سجل محدد
POST   tables/{table}              - إنشاء سجل
PUT    tables/{table}/{id}         - تحديث كامل
PATCH  tables/{table}/{id}         - تحديث جزئي
DELETE tables/{table}/{id}         - حذف سجل
```

---

## 👤 حسابات الاختبار | Demo Accounts

| الدور | اسم المستخدم | كلمة المرور |
|-------|--------------|-------------|
| المشرف العام | `admin` | `admin123` |
| مدير فرع | `manager_riyadh` | `manager123` |
| عامل ميداني | `worker1` | `worker123` |
| عميل | `client1` | `client123` |

---

## 🗂️ هيكل الملفات | File Structure

```
/
├── index.html              # الصفحة الرئيسية
├── login.html              # صفحة تسجيل الدخول
├── offline.html            # صفحة عدم الاتصال
├── manifest.json           # إعدادات PWA
├── sw.js                   # Service Worker
├── README.md               # هذا الملف
├── DEPLOYMENT-AI-CLASSIFICATION.md
├── css/
│   └── style.css           # الأنماط الرئيسية
├── js/
│   ├── app.js              # المنطق الرئيسي للتطبيق
│   ├── firebase-config.js  # إعدادات Firebase
│   ├── audit-sessions.js   # جلسات الجرد
│   ├── geolocation.js      # التسجيل الجغرافي
│   ├── photo-album.js      # نظام الصور
│   ├── export-enhanced.js  # التصدير المحسن
│   └── notifications.js    # الإشعارات
└── cloudflare-worker/
    └── worker.js           # Cloudflare Worker للذكاء الاصطناعي
```

---

## 🗄️ نموذج البيانات | Data Models

### الجداول في IndexedDB
| الجدول | الوصف |
|--------|--------|
| `assets` | الأصول |
| `departments` | الإدارات والأقسام |
| `maintenance` | طلبات الصيانة |
| `inventory_logs` | سجل عمليات الجرد |
| `settings` | الإعدادات |
| `sync_queue` | قائمة انتظار المزامنة |
| `activityLogs` | سجل النشاط |
| `assetLocations` | مواقع الأصول |
| `users` | المستخدمين |
| `branches` | الفروع |
| `audit_sessions` | جلسات الجرد |
| `session_scans` | مسحات الجلسات |
| `notifications` | الإشعارات |
| `photoAlbums` | ألبومات الصور |

### هيكل بيانات المستخدم
```javascript
{
  id: string,
  username: string,
  password: string,
  name: string,
  email: string,
  phone: string,
  role: 'admin' | 'manager' | 'field_user' | 'client',
  branch: string | null,
  employeeId: string,
  active: boolean,
  customPermissions: {
    canViewAllBranches: boolean,
    canManageUsers: boolean,
    canDeleteAssets: boolean,
    canExportData: boolean,
    canViewReports: boolean,
    canManageSessions: boolean
  },
  createdAt: ISO string,
  updatedAt: ISO string
}
```

### هيكل بيانات الأصل
```javascript
{
  id: string,
  code: string,
  name: string,
  category: string,
  category2: string,
  category3: string,
  department: string,
  building: string,
  floor: string,
  room: string,
  condition: string,
  supplier: string,
  purchaseDate: string,
  purchasePrice: number,
  currentValue: number,
  assignee: string,
  inventoryPerson: string,
  latitude: number,
  longitude: number,
  gpsAccuracy: number,
  googleMapsLink: string,
  images: Array,
  notes: string,
  createdAt: ISO string,
  updatedAt: ISO string
}
```

---

## ⚙️ إعدادات Firebase | Firebase Configuration

```javascript
const FIREBASE_CONFIG = {
    apiKey: "AIzaSyD4YmVKC1ByE-o5qWoeyHt5mIVM8v_v4ZM",
    authDomain: "aset-5be72.firebaseapp.com",
    projectId: "aset-5be72",
    storageBucket: "aset-5be72.firebasestorage.app",
    messagingSenderId: "4190814584",
    appId: "1:4190814584:web:c1e2354b04344a16db088d"
};
```

---

## 🚀 خطوات التشغيل | Getting Started

1. **افتح `login.html`** في المتصفح
2. **سجل الدخول** باستخدام أحد الحسابات التجريبية
3. **ابدأ استخدام النظام**:
   - أضف أصولاً جديدة
   - أنشئ جلسة جرد
   - صدّر التقارير

---

## 📱 تثبيت كتطبيق PWA

1. افتح النظام في متصفح Chrome أو Edge
2. ستظهر رسالة "تثبيت التطبيق" أو اضغط على زر التثبيت
3. يعمل التطبيق حتى بدون اتصال بالإنترنت

---

## 🔄 ما قيد التنفيذ | In Progress

- [ ] تحسين المزامنة المتقدمة مع Firebase
- [ ] تقارير الإهلاك المتقدمة
- [ ] اختبارات الأداء (Performance Testing)
- [ ] توثيق API كامل

---

## 📞 الدعم الفني | Support

للاستفسارات والدعم الفني، يرجى التواصل عبر نظام الإشعارات الداخلي.

---

**تاريخ آخر تحديث**: يناير 2025
**الإصدار**: 6.5.0
