# نظام جرد وحصر الأصول الحكومية
# Government Asset Inventory System

## الإصدار 6.0.0 - النسخة الشاملة مع Firebase Ready

---

## 📋 ملخص المشروع

نظام متكامل لإدارة وجرد وحصر الأصول في الجهات الحكومية، يعمل على الويب والهاتف المحمول مع دعم كامل للعمل بدون إنترنت (PWA) ومزامنة تلقائية مع السحابة.

---

## ✅ الميزات المكتملة (الإصدار 6.0)

### 🔐 نظام المصادقة والصلاحيات
- **ثلاثة مستويات من الصلاحيات:**
  - **المشرف العام (Admin):** وصول كامل لجميع الفروع والبيانات
  - **مدير الفرع (Manager):** صلاحيات محدودة في فرعه فقط
  - **العامل الميداني (Field User):** إضافة الأصول ورؤية أصوله فقط
- صفحة تسجيل دخول آمنة
- جلسات مستخدم مع انتهاء صلاحية تلقائي
- حسابات تجريبية للاختبار

### 📊 لوحة تحكم المشرف المتقدمة
- إحصاءات مباشرة (Real-time)
- متابعة نشاط العمال لحظياً
- رسوم بيانية لأداء الفروع
- خريطة توزيع الأصول بالإحداثيات
- مخطط التقدم اليومي
- إحصاءات الجلسات النشطة

### 📍 التسجيل الجغرافي (GPS)
- تحديد الموقع تلقائياً عند إضافة الأصول
- رابط Google Maps ديناميكي لكل أصل
- تضمين الروابط في التقارير والتصدير
- خريطة توزيع الأصول حسب المنطقة

### 📅 نظام جلسات الجرد
- إنشاء جلسات جرد لكل فرع
- ربط الأصول بالجلسة تلقائياً
- تتبع تقدم الجلسة
- إحصاءات أداء العمال
- تقارير مفصلة لكل جلسة

### 📸 نظام ألبوم الصور
- رفع صور متعددة لكل أصل
- معاينة الصور قبل الرفع
- رابط ألبوم خاص لكل أصل
- ضغط الصور تلقائياً

### 📤 نظام التصدير المحسن
- **تصدير Excel** مع:
  - روابط Hyperlinks لخريطة Google Maps
  - روابط ألبوم الصور
  - جميع بيانات الأصول
  - معلومات العامل والجلسة
- **تصدير PDF** (جاهز للطباعة)
- **تصدير CSV**

### 🔔 نظام الإشعارات
- إشعارات نجاح/فشل المزامنة
- تنبيهات أصول جديدة للمشرف
- إشعارات نشاط العمال
- تحذيرات تعارض البيانات
- دعم إشعارات سطح المكتب

### 🏢 إدارة الفروع
- إضافة وتعديل الفروع
- ربط المستخدمين بالفروع
- إحصاءات كل فرع
- تتبع الأصول حسب الفرع

### 👥 إدارة المستخدمين
- إنشاء حسابات المستخدمين
- تعيين الصلاحيات والفروع
- تفعيل/إلغاء تفعيل الحسابات
- تتبع نشاط المستخدمين

### 📱 دعم PWA كامل
- تثبيت كتطبيق على الهاتف
- العمل بدون إنترنت
- مزامنة تلقائية عند الاتصال
- تخزين محلي في IndexedDB

### 🔍 البحث والفلترة المتقدمة
- 12 فلتر بحث
- بحث متقدم قابل للطي
- قوائم منسدلة ذكية
- حفظ إعدادات الفلاتر

### 🤖 اقتراح التصنيف بالذكاء الاصطناعي
- تحليل صور الأصول
- اقتراح تصنيف ثلاثي المستويات
- مؤشر ثقة الاقتراح
- (يتطلب Cloudflare Worker)

---

## 🗂️ هيكل الملفات

```
/
├── index.html                    # الصفحة الرئيسية
├── login.html                    # صفحة تسجيل الدخول
├── offline.html                  # صفحة وضع عدم الاتصال
├── manifest.json                 # ملف PWA
├── sw.js                         # Service Worker
├── README.md                     # هذا الملف
├── DEPLOYMENT-AI-CLASSIFICATION.md  # وثائق نشر الذكاء الاصطناعي
│
├── css/
│   └── style.css                 # الأنماط الرئيسية
│
├── js/
│   ├── app.js                    # التطبيق الرئيسي
│   ├── firebase-config.js        # إعدادات Firebase
│   ├── audit-sessions.js         # نظام جلسات الجرد
│   ├── geolocation.js            # نظام GPS
│   ├── photo-album.js            # نظام ألبوم الصور
│   ├── export-enhanced.js        # التصدير المحسن
│   └── notifications.js          # نظام الإشعارات
│
└── cloudflare-worker/
    └── worker.js                 # Cloudflare Worker للذكاء الاصطناعي
```

---

## 🚀 نقاط الدخول (Entry Points)

| الصفحة | المسار | الوصف |
|--------|--------|-------|
| تسجيل الدخول | `/login.html` | نقطة الدخول الرئيسية |
| لوحة التحكم | `/index.html#dashboard` | لوحة التحكم الرئيسية |
| إدارة الأصول | `/index.html#assets` | قائمة وإدارة الأصول |
| جلسات الجرد | `/index.html#sessions` | إدارة جلسات الجرد |
| إدارة المستخدمين | `/index.html#users` | (للمشرف فقط) |
| إدارة الفروع | `/index.html#branches` | (للمشرف فقط) |
| التقارير | `/index.html#reports` | التقارير والتصدير |
| الإعدادات | `/index.html#settings` | إعدادات النظام |

---

## 👤 حسابات الاختبار

| الدور | اسم المستخدم | كلمة المرور |
|-------|-------------|-------------|
| المشرف العام | `admin` | `admin123` |
| مدير فرع | `manager_riyadh` | `manager123` |
| عامل ميداني | `worker1` | `worker123` |

---

## 💾 نموذج البيانات

### جدول الأصول (assets)
```javascript
{
  id: string,
  name: string,
  code: string,
  category: string,
  category2: string,
  category3: string,
  serialNumber: string,
  department: string,
  building: string,
  floor: string,
  room: string,
  location: string,
  purchasePrice: number,
  currentValue: number,
  purchaseDate: string,
  condition: string,
  supplier: string,
  warranty: string,
  assignee: string,
  inventoryPerson: string,
  lastInventoryDate: string,
  notes: string,
  images: array,
  // GPS Fields
  latitude: number,
  longitude: number,
  googleMapsLink: string,
  // Tracking Fields
  addedBy: string,
  addedByName: string,
  branch: string,
  sessionId: string,
  sessionName: string,
  createdAt: string,
  updatedAt: number
}
```

### جدول المستخدمين (users)
```javascript
{
  id: string,
  username: string,
  password: string,
  name: string,
  email: string,
  phone: string,
  role: 'admin' | 'manager' | 'field_user',
  branch: string,
  employeeId: string,
  active: boolean,
  createdAt: string
}
```

### جدول الفروع (branches)
```javascript
{
  id: string,
  name: string,
  code: string,
  address: string,
  phone: string,
  active: boolean
}
```

### جدول جلسات الجرد (audit_sessions)
```javascript
{
  id: string,
  name: string,
  branch: string,
  startDate: string,
  endDate: string,
  targetAssets: number,
  scannedAssets: number,
  participants: array,
  status: 'pending' | 'active' | 'paused' | 'completed',
  notes: string,
  createdBy: string,
  createdAt: string
}
```

---

## 🔧 التكامل مع Firebase (جاهز)

النظام مجهز للتكامل مع Firebase. لتفعيل المزامنة السحابية:

1. **إنشاء مشروع Firebase**
2. **تفعيل خدمات:**
   - Firebase Authentication
   - Firestore Database
   - Firebase Storage
   
3. **تحديث الإعدادات** في `js/firebase-config.js`:
```javascript
const FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
```

4. **تفعيل Firebase SDK** في `index.html` (إزالة التعليقات)

---

## 📊 التخزين المحلي

يستخدم النظام IndexedDB للتخزين المحلي:

| المخزن | الوصف |
|--------|-------|
| `assets` | بيانات الأصول |
| `departments` | الإدارات والأقسام |
| `users` | بيانات المستخدمين |
| `branches` | بيانات الفروع |
| `audit_sessions` | جلسات الجرد |
| `sync_queue` | قائمة المزامنة المعلقة |
| `notifications` | الإشعارات |
| `settings` | إعدادات النظام |

---

## 🎯 الخطوات التالية (مقترحة)

1. **تكامل Firebase الكامل:**
   - ربط المصادقة مع Firebase Auth
   - مزامنة الأصول مع Firestore
   - رفع الصور إلى Firebase Storage

2. **تحسينات إضافية:**
   - تقارير متقدمة مع تصفية بالتاريخ
   - نظام النسخ الاحتياطي التلقائي
   - لوحة إحصاءات تفاعلية

3. **اختبارات:**
   - اختبار الأداء مع بيانات كبيرة
   - اختبار التوافق مع المتصفحات
   - اختبار وضع عدم الاتصال

---

## 📝 سجل التغييرات

### الإصدار 6.0.0 (يناير 2026)
- ✅ نظام المصادقة والصلاحيات (Admin/Manager/Field User)
- ✅ نظام جلسات الجرد
- ✅ لوحة تحكم المشرف المتقدمة مع متابعة العمال
- ✅ التسجيل الجغرافي GPS مع روابط Google Maps
- ✅ نظام ألبوم الصور للأصول
- ✅ التصدير المحسن مع Hyperlinks
- ✅ نظام الإشعارات والتنبيهات
- ✅ إدارة الفروع والمستخدمين
- ✅ رسوم بيانية لأداء الفروع والتقدم اليومي
- ✅ خريطة توزيع الأصول

### الإصدار 5.3.0
- اقتراح التصنيف بالذكاء الاصطناعي
- تحسين واجهة المستخدم

### الإصدار 5.2.0
- البحث المتقدم مع 12 فلتر
- القوائم المنسدلة الذكية

### الإصدار 5.0.0
- استيراد البيانات من Excel
- تحديد موقع المبنى/الطابق/الغرفة

---

## 🛠️ المتطلبات التقنية

- **المتصفح:** Chrome 80+, Firefox 75+, Safari 13+, Edge 80+
- **الهاتف:** iOS 13+, Android 8+
- **الاتصال:** يعمل بدون إنترنت (PWA)
- **التخزين:** 50MB+ متاح للتخزين المحلي

---

## 📞 الدعم الفني

للمساعدة أو الإبلاغ عن مشاكل، يرجى التواصل مع فريق الدعم التقني.

---

**تم التطوير لخدمة الجهات الحكومية** 🏛️
