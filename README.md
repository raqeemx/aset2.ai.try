# نظام جرد وحصر الأصول الحكومية
## Government Asset Inventory Management System

![Version](https://img.shields.io/badge/version-6.0.0-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)
![Language](https://img.shields.io/badge/language-Arabic-orange.svg)
![PWA](https://img.shields.io/badge/PWA-Ready-purple.svg)
![Offline](https://img.shields.io/badge/offline-supported-success.svg)
![Firebase](https://img.shields.io/badge/Firebase-Ready-yellow.svg)
![AI](https://img.shields.io/badge/AI-OpenAI%20Vision-blueviolet.svg)

نظام متكامل واحترافي لإدارة وجرد وحصر الأصول في الجهات الحكومية، يوفر واجهة مستخدم عربية سهلة الاستخدام مع ميزات متقدمة للتتبع والتقارير. **يعمل بدون إنترنت ويحفظ البيانات تلقائياً مع المزامنة السحابية!**

---

## 🆕 الميزات الجديدة في الإصدار 6.0

### 🔐 نظام المصادقة والصلاحيات
- **تسجيل دخول آمن** عبر Firebase Authentication
- **ثلاثة أنواع من المستخدمين** بصلاحيات مختلفة:
  - **المشرف العام (Admin)**: صلاحيات كاملة على جميع الفروع والمستخدمين
  - **مدير الفرع (Manager)**: إدارة فرع واحد وعماله
  - **العامل الميداني (Field User)**: إضافة أصول فقط ورؤية ما أضافه
- **حماية البيانات** حسب الصلاحية

### 📋 نظام جلسات الجرد
- **إنشاء جلسات جرد** منظمة لكل فرع
- **تتبع التقدم** في الوقت الفعلي
- **إحصائيات المشاركين** وإنتاجيتهم
- **تقارير لكل جلسة** على حدة

### ☁️ المزامنة السحابية مع Firebase
- **مزامنة تلقائية** عند توفر الإنترنت
- **حفظ محلي** عند عدم الاتصال
- **حل التعارضات** تلقائياً
- **تحديثات في الوقت الفعلي** للمشرفين

### 📍 تسجيل الموقع الجغرافي GPS
- **التقاط إحداثيات GPS** تلقائياً مع كل أصل
- **روابط Google Maps** مباشرة في التقارير
- **عرض خريطة** داخل النظام
- **حساب المسافات** بين الأصول

### 📸 نظام ألبوم الصور
- **رفع صور متعددة** لكل أصل
- **تخزين سحابي** عبر Firebase Storage
- **ألبوم صور** لكل أصل
- **روابط الألبوم** في التقارير المُصدّرة

### 📊 تقارير محسّنة مع Hyperlinks
- **روابط الخرائط** قابلة للنقر في Excel
- **روابط ألبوم الصور** في التقارير
- **تصدير PDF** احترافي
- **تصدير CSV** مع ترميز UTF-8

### 🔔 نظام الإشعارات
- **إشعارات المزامنة** (نجاح/فشل)
- **إشعارات الأصول الجديدة** للمشرفين
- **تنبيهات التعارضات**
- **إشعارات Push** (PWA)

---

## 📁 هيكل الملفات

```
├── index.html              # الصفحة الرئيسية
├── login.html              # صفحة تسجيل الدخول
├── offline.html            # صفحة عدم الاتصال
├── manifest.json           # PWA manifest
├── sw.js                   # Service Worker
├── css/
│   └── style.css           # الأنماط المخصصة
├── js/
│   ├── app.js              # التطبيق الرئيسي
│   ├── firebase-config.js  # إعدادات Firebase
│   ├── auth.js             # نظام المصادقة
│   ├── audit-sessions.js   # جلسات الجرد
│   ├── sync-manager.js     # إدارة المزامنة
│   ├── gps-location.js     # الموقع الجغرافي
│   ├── photo-album.js      # ألبوم الصور
│   ├── enhanced-export.js  # التصدير المحسّن
│   ├── notifications.js    # الإشعارات
│   └── user-management.js  # إدارة المستخدمين
├── worker.js               # Cloudflare Worker للذكاء الاصطناعي
├── DEPLOYMENT-AI-CLASSIFICATION.md
└── README.md
```

---

## 🚀 خطوات التشغيل

### المتطلبات الأساسية
1. متصفح حديث يدعم PWA
2. حساب Firebase (اختياري للمزامنة السحابية)
3. حساب OpenAI (اختياري لميزة الذكاء الاصطناعي)

### التشغيل بدون Firebase (الوضع المحلي)
1. افتح `login.html` في المتصفح
2. استخدم أحد الحسابات التجريبية:
   - **المشرف**: admin / 123456
   - **المدير**: manager / 123456
   - **العامل الميداني**: field_user / 123456
3. جميع البيانات ستُحفظ محلياً في IndexedDB

### التشغيل مع Firebase (مزامنة سحابية)

#### الخطوة 1: إنشاء مشروع Firebase
1. اذهب إلى [Firebase Console](https://console.firebase.google.com)
2. أنشئ مشروعاً جديداً
3. فعّل الخدمات التالية:
   - **Authentication** (Email/Password)
   - **Cloud Firestore**
   - **Storage**

#### الخطوة 2: الحصول على إعدادات المشروع
1. اذهب إلى **Project Settings** > **General**
2. في قسم "Your apps"، أضف تطبيق ويب
3. انسخ إعدادات Firebase

#### الخطوة 3: تحديث ملف الإعدادات
افتح `js/firebase-config.js` واستبدل القيم:

```javascript
const FIREBASE_CONFIG = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_PROJECT_ID.appspot.com",
    messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
    appId: "YOUR_APP_ID"
};
```

#### الخطوة 4: إعداد قواعد الأمان في Firestore

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users collection
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && 
                   get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin';
    }
    
    // Assets collection
    match /assets/{assetId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null;
      allow update, delete: if request.auth != null && 
                            (resource.data.createdBy == request.auth.uid || 
                             get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role in ['admin', 'manager']);
    }
    
    // Audit sessions
    match /auditSessions/{sessionId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && 
                   get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role in ['admin', 'manager'];
    }
    
    // Activity logs
    match /activityLogs/{logId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null;
    }
  }
}
```

#### الخطوة 5: إعداد قواعد Storage

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /assets/{assetId}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
```

#### الخطوة 6: إنشاء حساب المشرف الأول
1. اذهب إلى **Authentication** > **Users**
2. أضف مستخدماً جديداً بالبريد وكلمة المرور
3. في **Firestore**، أنشئ مجموعة `users` وأضف وثيقة:

```json
{
  "username": "admin",
  "displayName": "المشرف العام",
  "email": "admin@example.com",
  "role": "admin",
  "branch": null,
  "isActive": true,
  "createdAt": "2026-01-01T00:00:00Z"
}
```

---

## 🤖 إعداد ميزة الذكاء الاصطناعي (اختياري)

راجع ملف `DEPLOYMENT-AI-CLASSIFICATION.md` للتعليمات الكاملة.

**ملخص سريع:**
1. أنشئ حساب Cloudflare Workers
2. انشر ملف `worker.js`
3. أضف مفتاح OpenAI كـ Secret
4. حدّث `AI_API_BASE` في `js/app.js`

---

## 📱 تثبيت التطبيق (PWA)

### على الهاتف (Android/iOS)
1. افتح الموقع في Chrome/Safari
2. اضغط على "إضافة للشاشة الرئيسية"
3. التطبيق سيعمل كتطبيق مستقل

### على الكمبيوتر
1. افتح الموقع في Chrome
2. اضغط على أيقونة التثبيت في شريط العنوان
3. أو اضغط زر "تثبيت التطبيق" في الشريط الجانبي

---

## 🔒 نظام الصلاحيات

### المشرف العام (Admin)
| الصلاحية | مُفعّلة |
|----------|---------|
| عرض جميع الأصول | ✅ |
| عرض جميع الفروع | ✅ |
| إدارة المستخدمين | ✅ |
| إنشاء جلسات الجرد | ✅ |
| تعديل/حذف أي أصل | ✅ |
| التقارير الشاملة | ✅ |
| سجل المراجعة | ✅ |

### مدير الفرع (Manager)
| الصلاحية | مُفعّلة |
|----------|---------|
| عرض أصول فرعه | ✅ |
| إنشاء جلسات لفرعه | ✅ |
| تقارير الفرع | ✅ |
| متابعة عمال الفرع | ✅ |
| إدارة المستخدمين | ❌ |
| عرض فروع أخرى | ❌ |

### العامل الميداني (Field User)
| الصلاحية | مُفعّلة |
|----------|---------|
| إضافة أصول | ✅ |
| عرض أصوله فقط | ✅ |
| العمل بدون إنترنت | ✅ |
| تعديل أصول الآخرين | ❌ |
| إنشاء جلسات | ❌ |
| التقارير | ❌ |

---

## 📊 نموذج البيانات

### الأصل (Asset)
```javascript
{
  id: string,
  code: string,              // رقم الأصل
  name: string,              // اسم الأصل
  category: string,          // الفئة الرئيسية
  category2: string,         // الفئة الفرعية
  category3: string,         // الفئة التفصيلية
  condition: string,         // الحالة
  location: string,          // الموقع
  building: string,          // المبنى
  floor: string,             // الطابق
  room: string,              // الغرفة
  department: string,        // القسم
  assignee: string,          // المسؤول
  value: number,             // القيمة
  purchaseDate: string,      // تاريخ الشراء
  supplier: string,          // المورد
  serialNumber: string,      // الرقم التسلسلي
  notes: string,             // ملاحظات
  images: Array,             // الصور
  gpsLocation: {             // الموقع الجغرافي
    latitude: number,
    longitude: number,
    accuracy: number,
    mapLink: string
  },
  sessionId: string,         // جلسة الجرد
  branch: string,            // الفرع
  createdBy: string,         // أنشئ بواسطة
  createdByName: string,     // اسم المنشئ
  createdAt: string,         // تاريخ الإنشاء
  updatedAt: string          // تاريخ التحديث
}
```

### جلسة الجرد (AuditSession)
```javascript
{
  id: string,
  name: string,              // اسم الجلسة
  description: string,       // الوصف
  branch: string,            // الفرع
  branchName: string,        // اسم الفرع
  status: string,            // الحالة (draft/active/paused/completed/cancelled)
  startDate: string,         // تاريخ البدء
  endDate: string,           // تاريخ الانتهاء
  targetAssetCount: number,  // العدد المستهدف
  scannedAssetCount: number, // العدد الممسوح
  participants: Array,       // المشاركين
  createdBy: string,         // أنشئت بواسطة
  createdAt: string
}
```

### المستخدم (User)
```javascript
{
  id: string,
  username: string,          // اسم المستخدم
  displayName: string,       // الاسم الكامل
  email: string,             // البريد الإلكتروني
  phone: string,             // الهاتف
  role: string,              // الصلاحية (admin/manager/field_user)
  branch: string,            // الفرع
  branchName: string,        // اسم الفرع
  isActive: boolean,         // نشط
  createdAt: string,
  stats: {
    totalAssets: number,
    lastActivity: string
  }
}
```

---

## 🛠️ الميزات التقنية

- **PWA**: Progressive Web App كامل
- **Offline First**: يعمل بدون إنترنت
- **IndexedDB**: تخزين محلي سريع
- **Firebase Firestore**: قاعدة بيانات سحابية
- **Firebase Storage**: تخزين الصور
- **Firebase Auth**: مصادقة آمنة
- **Geolocation API**: تحديد المواقع
- **Web Workers**: معالجة في الخلفية
- **Push Notifications**: إشعارات فورية

---

## 🔄 آلية المزامنة

```
┌─────────────────┐     ┌──────────────────┐
│   المستخدم      │────▶│   IndexedDB      │
│   (إضافة أصل)   │     │   (حفظ محلي)     │
└─────────────────┘     └────────┬─────────┘
                                 │
                    ┌────────────▼────────────┐
                    │    فحص الاتصال          │
                    └────────────┬────────────┘
                                 │
              ┌──────────────────┴──────────────────┐
              │                                     │
     ┌────────▼────────┐               ┌───────────▼───────────┐
     │   غير متصل      │               │      متصل            │
     │   (إضافة للقائمة)│               │   (رفع مباشر)        │
     └────────┬────────┘               └───────────┬───────────┘
              │                                     │
              │                         ┌───────────▼───────────┐
              │                         │   Firebase Firestore  │
              │                         └───────────┬───────────┘
              │                                     │
              └──────────────┬──────────────────────┘
                             │
                   ┌─────────▼─────────┐
                   │   عند عودة الاتصال │
                   │   رفع القائمة      │
                   └───────────────────┘
```

---

## 📞 الدعم والتواصل

للأسئلة والدعم الفني، يرجى التواصل عبر:
- فتح Issue في المستودع
- البريد الإلكتروني للدعم

---

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT.

---

## 🙏 شكر وتقدير

- [Tailwind CSS](https://tailwindcss.com)
- [Font Awesome](https://fontawesome.com)
- [Chart.js](https://www.chartjs.org)
- [Firebase](https://firebase.google.com)
- [SheetJS](https://sheetjs.com)

---

**تم التطوير بـ ❤️ للجهات الحكومية العربية**
